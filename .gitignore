node_modules/
dist/
.wrangler/
.dev.vars
*.log
.DS_Store

# UI build output / deps
ui/node_modules/
ui/dist/
ui/.vite/

# local scratch
/tmp-*

# signing keys / secrets — never commit (set prod keys via `wrangler secret put`)
*.key
*.pem
